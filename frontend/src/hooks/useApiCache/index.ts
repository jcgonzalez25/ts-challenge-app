// Main useApiCache hook export
export { useApiCache } from './useApiCache';

// Type exports
export type {
  CacheConfig,
  ApiCacheOperations,
  UseApiCacheConfig,
} from './types';

// Default export for convenience
export { useApiCache as default } from './useApiCache'; 